with "../../gplimports/gplimports" ;

project qclient is                           -- [template/$]

   for Object_Dir use "../../build";

   for Languages use ("Ada");
   package Compiler is
      for Switches ("Ada")
          use ("-O2","-gnat05");
   end Compiler;

   for Main use ("queue-client.adb") ;              -- [clitest/$]
end qclient ;                                 -- [template/$]
